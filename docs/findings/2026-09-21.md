# Findings — hasansa007/dev-desk — 2026-09-21

Flows: 1, `findings`, declared by `skills/findings/SKILL.md:2`. Scope: findings door and directly invoked contracts/callers; bugs and architecture. Tier: Deep (explicit adversarial review).

Arguments: `findings --plan=decide --walkthrough=skip --file=skip --max-agents=40`.

Source: `b9f8c01bd35d2817cc220ceb8efe87f833ba2160`. Report branch: `docs/findings-flow-2026-09-21`, attached at the inspected HEAD in the existing dedicated task worktree; HEAD matched `origin/main`. The source repository's own workflow exemption applies. No implementation changes, deployment, app build, or GitHub writes were performed.

The original all-flow plan was refused before agents started. The developer then selected `findings` after being asked to choose a flow. This is that narrowed run, not an audit of the Findings screen or the whole application.

## COVERAGE

Discovery used tracked-file enumeration (`git ls-files`), not a directory walk. The selected implementation is `skills/findings/SKILL.md`; direct contracts, filing delegates, ideation's imports, and CLI dispatch were inspected as dependencies. Checkers also verified the app's `--arch` argument construction in `apps/desk/DevDesk/Sheets/RunFocusSheet.swift`.

No `docs/flows.md` exists, so no existing registry needed aliases added. No tracked prior reports were found under `docs/findings/` or `docs/survey/`; none were used as evidence. `docs/findings/` was created for this report. `git check-ignore -q docs/` and the report-path probe both exited 1: these paths are not ignored.

Excluded nested projects: `tests/e2e-project/` and `tests/nextjs-new/` (package.json), `tests/android-new/` (build.gradle), `tests/ios-new/` and `apps/desk/DeskCore/` (Package.swift). Other doors and app actions were not independently audited.

Verification establishes instruction-level contradictions and local command behavior. It does not establish that an agent actually filed duplicates, hung, bypassed verification, or edited the wrong checkout.

## GROUPS (0)

Each fix can ship independently. Sharing one skill file does not require atomic delivery. No dependency chain or cross-layer outcome was established; no group branch is proposed.

## CONFIRMED (6)

1. Per-path duplicate searches silently retain a 30-result ceiling.
   id: C1   type: Logic   group: none
   evidence: skills/findings/SKILL.md:91; shared/duplicates.md:16; skills/ideation/SKILL.md:62
   mechanism: The prescribed gh issue list search omits --limit; installed gh help reports a default of 30. With 31 or more matching issues, a same-fix issue outside the returned subset is invisible. Findings' explanation at lines 95–98 suggests searching removed truncation, and the filing rule can classify the unseen match as new work.
   expected: Exhaust the matching result set or explicitly treat incomplete search coverage as unresolved deduplication.
   touches: skills/findings/SKILL.md › Phase 2 dedupe query and coverage explanation; shared/duplicates.md › section 1 tracker query; skills/ideation/SKILL.md › Phase 2 all-state query
   shares: C2 skills/findings/SKILL.md › Phase 2 query versus Phase 5 overrun rule — different code, parallel
   shares: C3 skills/findings/SKILL.md › Phase 2 query versus Phase 3 registry and Phase 7 write sequencing — different code, parallel
   shares: C4 skills/findings/SKILL.md › Phase 2 query versus Phase 1 arch flag and Phase 7c scope wording — different code, parallel
   shares: C5 skills/findings/SKILL.md › Phase 2 query versus tracker-selection/error clause — different code, parallel
   limits: The command cap is verified locally. No 31-issue tracker fixture or actual duplicate filing was reproduced. The global scope-limiter rule mitigates the problem if an agent notices the misleading local guidance.
   priority: P1 proposed; impact high, complexity low. Ranked first because repeated false-new filings pollute the tracker and affect both findings and its consumers.

2. Checker-overrun approval conflicts with an explicitly preapproved run.
   id: C2   type: Logic   group: none
   evidence: skills/findings/SKILL.md:205; shared/pipeline/00-principles.md:67; docs/adr/0043-a-findings-run-is-told-its-stops-before-it-starts.md:23
   mechanism: Phase 5 unconditionally says to stop and re-ask above twice the estimated checker count. With one finder, two estimated checkers, three candidates requiring six checkers, and max-agents 40, that condition fires while total usage is only seven. The decide contract preapproves spending up to the hard limit and says to report instead of asking.
   expected: One consistent stop policy for an all-decide/skip run, preserving the hard cap and recording any overrun.
   touches: skills/findings/SKILL.md › Phase 5 checker-overrun rule
   shares: C1 skills/findings/SKILL.md › Phase 5 overrun versus Phase 2 query — different code, parallel
   shares: C3 skills/findings/SKILL.md › Phase 5 overrun versus Phase 3 registry and Phase 7 write sequencing — different code, parallel
   shares: C4 skills/findings/SKILL.md › Phase 5 overrun versus Phase 1 arch flag and Phase 7c scope wording — different code, parallel
   shares: C5 skills/findings/SKILL.md › Phase 5 overrun versus tracker-selection/error clause — different code, parallel
   limits: Both checkers confirmed incompatible instructions, not an inevitable background stall. Ideation inherits Phase 5. An agent that prioritizes preauthorization can proceed correctly.
   priority: P2 proposed; impact high, complexity low.

3. Architecture-only scope contradicts the mandatory verification rule.
   id: C4   type: Logic   group: none
   evidence: skills/findings/SKILL.md:40; skills/findings/SKILL.md:197; skills/findings/SKILL.md:299; apps/desk/DevDesk/Sheets/RunFocusSheet.swift:252
   mechanism: The --arch argument definition and report instructions explicitly skip Phase 5. Phase 5 explicitly says it is never skipped, including for architecture counts and drift. The app emits this flag, so the conflicting route is reachable.
   expected: Architecture-only skips bug hunting while preserving independent verification of architecture claims.
   touches: skills/findings/SKILL.md › Phase 1 arch argument definition; skills/findings/SKILL.md › Phase 7c flag-specific section rules
   shares: C1 skills/findings/SKILL.md › arch flag/report scope versus Phase 2 query — different code, parallel
   shares: C2 skills/findings/SKILL.md › arch flag/report scope versus Phase 5 overrun — different code, parallel
   shares: C3 skills/findings/SKILL.md › Phase 7c scope clause versus Phase 7c write-boundary clause and Phase 3 registry — different code, parallel
   shares: C5 skills/findings/SKILL.md › arch flag/report scope versus tracker-selection/error clause — different code, parallel
   limits: A careful runner can follow the explicit verification requirement. No unchecked epic was observed or filed.
   priority: P2 proposed; impact medium, complexity low.

4. The disabled-Issues stop blocks the advertised local group-filing route.
   id: C5   type: Logic   group: none
   evidence: skills/findings/SKILL.md:101; skills/findings/SKILL.md:476; shared/duplicates.md:20
   mechanism: The skill says to stop if Issues are disabled, without distinguishing intentional absence of a tracker from a failed query. Its local group-filing contract nevertheless promises parent and ticket entries in docs/backlog/ without a tracker. Local dedupe exists, but there is no routing exception reconciling it with the stop.
   expected: A defined local route for intentionally absent trackers, while preserving stop-on-failed-search behavior for configured trackers.
   touches: skills/findings/SKILL.md › Phase 2 tracker-selection/error clause; skills/findings/SKILL.md › Phase 9 local-filing handoff
   shares: C1 skills/findings/SKILL.md › tracker-selection/error clause versus Phase 2 query — different code, parallel
   shares: C2 skills/findings/SKILL.md › tracker selection/local handoff versus Phase 5 overrun — different code, parallel
   shares: C3 skills/findings/SKILL.md › tracker selection/local handoff versus Phase 3 registry and Phase 7 write sequencing — different code, parallel
   shares: C4 skills/findings/SKILL.md › tracker selection/local handoff versus arch flag/report scope — different code, parallel
   limits: Dedupe is deferred to Phase 9, so this does not prove report generation is blocked. It does not establish that no-remote runs always fail or that Dev Desk's direct local capture is broken. No disabled-Issues repository was exercised.
   priority: P2 proposed; impact medium, complexity medium.

5. Flow discovery writes a registry despite the report-only write contract.
   id: C3   type: Logic   group: none
   evidence: skills/findings/SKILL.md:120; skills/findings/SKILL.md:316; skills/ideation/SKILL.md:71
   mechanism: If an existing docs/flows.md lacks a discovered ID, Phase 3 instructs adding it. That precedes the fan-out decision and can leave a registry change even when the plan is later refused. Phase 7 says it is the one phase that is not read-only.
   expected: The registry-write timing and the declared write phases agree, with the shared isolation boundary honored.
   touches: skills/findings/SKILL.md › Phase 3 flow-registry reconciliation; skills/findings/SKILL.md › Phase 7c write sequencing
   shares: C1 skills/findings/SKILL.md › registry/write sequencing versus Phase 2 query — different code, parallel
   shares: C2 skills/findings/SKILL.md › registry/write sequencing versus Phase 5 overrun — different code, parallel
   shares: C4 skills/findings/SKILL.md › Phase 7c write-boundary clause versus Phase 7c scope clause and arch flag — different code, parallel
   shares: C5 skills/findings/SKILL.md › registry/write sequencing versus tracker selection/local handoff — different code, parallel
   limits: The stronger claim of required unsafe checkout mutation was not confirmed: shared/entry.md already governs all writes. The fan-out gate approves spending, not necessarily every filesystem edit. No registry was changed in this run.
   priority: P3 proposed; impact low, complexity low.

6. Dispatcher options between the door and its arguments break argument forwarding.
   id: C6   type: Logic   group: none
   evidence: scripts/dev.py:873; scripts/dev.py:891; docs/guide/COMMANDS.md:116
   mechanism: An optional door positional and nargs-star argument positional are parsed with ordinary parse_args. After an intervening --agent option, parsing does not resume the positional arguments. Parser-only execution rejects run findings --agent codex -- --bugs and run findings --agent codex checkout with exit 2.
   expected: Supported door arguments can be forwarded with a clear, documented ordering contract.
   touches: scripts/dev.py › build_parser() run argument contract; tests/state/test_dev_run.py › parser coverage; docs/guide/COMMANDS.md › dev run syntax
   limits: run --agent codex findings -- --bugs succeeds, as does run findings -- --bugs. Bare --bugs rejection alone is normal delimiter behavior. Arbitrary intermixing is not explicitly promised; the confirmed defect is a modest usability/forwarding limitation, not inability to run findings with flags.
   priority: P3 proposed; impact low, complexity low.

## PLAUSIBLE (0)

No independent candidate remains held after narrowing the six findings to what both checkers established. The unproven runtime consequences are explicitly bounded on their corresponding entries.

## REFUTED (0 whole candidates)

All six candidates survived in narrower form. The following stronger interpretations were rejected or left unproven; they are not additional tickets:

| Entry | Stronger claim | Disposition and reason |
| --- | --- | --- |
| C2 | Every decide-mode run necessarily hangs on overrun | Unproven: an agent can prioritize the explicit preapproval rule. |
| C3 | Phase 3 requires writing to the developer's original checkout | Refuted as a requirement: the previously loaded shared boundary already requires isolation. |
| C4 | Architecture-only necessarily files unchecked work | Unproven: Phase 5 and confirmed-only filing provide contrary instructions. |
| C5 | Every no-tracker run stops before producing a report | Unproven: dedupe is deferred, and local search can avoid a remote query. |
| C6 | Findings flags cannot be forwarded at all | Refuted: parser-only reproduction succeeds with the alternate ordering. |

## ARCHITECTURE

Actual: one tracked implementation file under skills/findings/, a prose orchestrator with nine top-level numbered phases and three Phase 7 subdivisions. Counted from git ls-files and source headings, then independently recounted by two checkers. It imports shared entry, principles, universal rules and deduplication, and delegates bug/epic/ADR filing to existing doors. Ideation imports the protocol rather than providing a second findings implementation.

- The scoped flow consistently uses the existing prose-orchestration pattern.
  id: A1   type: Architecture   group: none
  touches: skills/findings/SKILL.md › phase orchestration (inspected; no structural change proposed)
  evidence: skills/findings/SKILL.md:79; skills/findings/SKILL.md:450; skills/findings/SKILL.md:455; skills/findings/SKILL.md:462; skills/ideation/SKILL.md:47
  recommendation: Retain this pattern; no architectural move, ADR, or epic is justified by this one-flow sample.
  doing-nothing: No separate structural cost was established. The behavioral contract defects above remain actionable without a rewrite.

No drift items were established. These counts do not describe the whole repository's architecture.

## ALREADY TRACKED (0 matches in the performed query)

The explicit per-path query for skills/findings/SKILL.md returned an empty JSON list after a sandbox-network retry. This supports only that query's result, not a complete duplicate-free claim for all touched paths. No tracker matching or merging was performed for the other paths; Phase 9 filing is skipped. There were no tracked local-backlog or prior-report files in the enumerated locations. Before future filing, repeat dedupe per touched path with complete coverage.

## DISPOSITION

| id | Fate |
| --- | --- |
| C1 | Report only; filing skipped. |
| C2 | Report only; filing skipped. |
| C3 | Report only; filing skipped. |
| C4 | Report only; filing skipped. |
| C5 | Report only; filing skipped. |
| C6 | Report only; filing skipped. |
| A1 | Retain current architecture; no work item proposed. |

## NET

opened 0 · merged into existing 0 · placed in a milestone 0 · unplaced filed items 0 · open issues not measured → not measured. All six defect entries remain unfiled by request; no milestones or labels were changed.

## COST

Decision: proceed with the user-selected findings flow under --plan=decide because the initial estimate fits the 40-agent limit. The refused broad plan started no agents and is not included in the narrowed run's agent count.

Declared: one finder, approximately three candidate defects requiring six checkers, plus two independent architecture checkers = nine agents. This was an estimate, not a bound.

Actual: one finder + twelve defect checkers + two architecture checkers = fifteen agents. Overrun: six agents, because the finder returned six candidates rather than three. Fourteen checkers did not exceed twice the declared eight-checker estimate; total agents remained below 40. Two spawn attempts and one follow-up attempt were rejected by the harness thread limit; they started no work, and new checks were dispatched after slots freed. No finder or checker failed after starting.

Measured coordinator interval: 22:19:32–22:28:13 UTC on September 20 (01:19:32–01:28:13 September 21 in Asia/Jerusalem), 8 minutes 41 seconds through the initial report validation, including permission waits and report preparation. Final saving adds to that interval. Exact aggregate agent duration, token counts, and total tool-call counts are unavailable. They are not inferred from message lengths. Concurrent durations must not be summed as wall time.

| Per flow | Findings | Completion | Duration | Tokens | Calls |
| --- | --- | --- | --- | --- | --- |
| findings | 6 candidates; 6 confirmed after narrowing | DONE, not a failed or empty finder | 66 seconds | unavailable | 19 shell calls in 8 orchestration calls; 1 progress message |

| Independent check | Verdict | Reported cost evidence |
| --- | --- | --- |
| Architecture A / B | Both confirm count and scoped retention | A: 6 shell + 3 clock calls, partial measured interval 21 s; B: 7 shell calls, approximately 20 s. |
| C1 A / B | Both confirm bounded search, not actual duplicate filing | A: 3 shell calls, approximately 35 s; B: 4 tool calls, full duration unavailable. |
| C2 A / B | Both confirm conflicting instructions, not inevitable stall | A: 3 tool calls, approximately 1 min; B: 3 tool calls, full duration unavailable. |
| C3 A / B | Both confirm contradictory write timing, not required unsafe checkout | A: 2 shell calls; B: 2 tool calls; full durations unavailable. |
| C4 A / B | Both confirm conflicting arch verification instructions | A: 3 tool calls, approximately 1 min; B: 4 tool calls, duration unavailable. |
| C5 A / B | Both confirm blocked advertised local-filing route | A: metrics unavailable; B: 3 local calls; durations unavailable. |
| C6 A / B | Both confirm ordering limitation; alternate ordering works | A: 3 tool calls, approximately 1 min; B: metrics unavailable. |

Metrics use the units each checker actually reported and are not an aggregate bill. Two independent reviewers opened source and callers for every candidate; neither received its partner's verdict. No prior findings report supplied evidence.

## STOPS

Phase 4: decide — selected flow and estimate recorded above. Phase 8: skipped by explicit argument. Phase 9: skipped by explicit argument; no issues, comments, epics, ADRs, or local backlog tickets were created. The report is the result.
